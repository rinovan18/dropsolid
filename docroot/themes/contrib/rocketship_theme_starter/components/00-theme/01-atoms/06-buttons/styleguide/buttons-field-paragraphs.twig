{#
/**
 * This is strictly an example file to demonstrate markup in Storybook.
 * It is not intended to be included in any other component.
 */
#}

{#
  We're flipping things upside down a little bit, by embedding the dummy Paragraph component in here and only rendering the buttons field
  That way, we can show the Paragraphs Buttons in combination with the background-color variations
#}
{% include "@rocketship-theme-starter-p-organisms/00-paragraphs/styleguide/paragraphs.twig" with {
  label: label,
  configuration: configuration,
  bem_block_modifiers: bem_block_modifiers,
  bem_block_extra: bem_block_extra,
  paragraphs: paragraphs,
} %}

